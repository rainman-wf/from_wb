<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/root">

    <fragment
        android:id="@+id/root"
        android:name="ru.netology.timetotravel.fragments.Root"
        android:label="fragment_root"
        tools:layout="@layout/fragment_root" >
        <action
            android:id="@+id/action_root_to_itemDetails"
            app:destination="@id/itemDetails" />
    </fragment>
    <fragment
        android:id="@+id/itemDetails"
        android:name="ru.netology.timetotravel.fragments.ItemDetails"
        android:label="fragment_item_details"
        tools:layout="@layout/fragment_item_details" />
</navigation>